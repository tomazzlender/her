# HER plugin for IntelliJ-platform IDEs (thin / LSP)

A minimal plugin with two halves:

- **Highlighting** — it ships the HER TextMate grammar and registers it
  through the IDE's bundled TextMate support, so `.her` files (including
  `.html.her`) are recognized and colored as soon as the plugin is
  installed. No manual bundle import.
- **Language server** — it starts `her lsp` for any project with open
  `.her` files and lets the IDE render what the server knows:
  diagnostics as you type plus `Her.verify` findings, completion
  (components, attrs with types, slots), hover, and go-to-definition.

**Requires a commercial JetBrains IDE** (IntelliJ IDEA Ultimate,
RubyMine, ...) 2024.2 or newer — the IntelliJ Platform LSP API does not
exist in Community Edition. On Community, use the
[LSP4IJ](https://plugins.jetbrains.com/plugin/23257-lsp4ij) plugin
instead and define a server with the same command (see
[`../../README.md`](../../README.md)).

## Build & install

Needs JDK 21 and Gradle 8.10+ (this skeleton is not built by the repo's
Ruby CI):

```sh
cd editors/intellij/her
gradle buildPlugin
# -> build/distributions/her-intellij-0.1.0.zip
```

Then `Settings → Plugins → ⚙ → Install Plugin from Disk…` and pick the
zip. `gradle runIde` launches a sandboxed IDE for trying it out.

The compatibility range is open-ended (`sinceBuild 242`, no until-build),
so the zip installs on any 2024.2+ IDE. If a future platform release ever
breaks the LSP API at runtime, bump `intellijIdeaUltimate("2024.2")` in
`build.gradle.kts` to your IDE's version and rebuild.

## How the server is started

One process per project, in the project root:

- `bundle exec her lsp ...` when a `Gemfile` exists at the root,
  plain `her lsp ...` otherwise;
- the boot file (`-r`, which loads your component modules) comes from a
  `.her-lsp` file at the project root — its first non-comment line is
  the path — or defaults to `config/boot.rb` when that file exists.
  Without one you still get syntax diagnostics.

```sh
echo "app/boot.rb" > .her-lsp   # check it in; the whole team shares it
```

## Syntax highlighting

Bundled. The plugin carries the grammar as a classic `.tmbundle`
(`src/main/resources/textmate/her.tmbundle/`; the `Syntaxes/her.tmLanguage`
inside is generated by `rake grammar`) and hands it to the IDE's TextMate
plugin via the `textmate.bundleProvider` extension point. Holes highlight
as plain text unless a Ruby TextMate bundle is also loaded; the
HTML/component/slot structure highlights regardless.

This rides on the **TextMate Bundles** plugin, which is bundled and
enabled by default in every IntelliJ-platform IDE — if you disabled it,
re-enable it (the dependency is optional, so the LSP half works either
way).

## Troubleshooting

**`.her` files still look like plain text after installing.**

1. Check `Settings → Editor → File Types` for a manual association: if
   `*.her` or `*.html.her` was ever assigned to a native file type (the
   IDE offers "Text" when you open an unknown extension and it is easy
   to click through), that explicit mapping shadows the TextMate
   grammar. Remove the pattern from whatever type owns it.
2. Make sure the *TextMate Bundles* plugin is enabled
   (`Settings → Plugins → Installed`).
3. Restart the IDE after installing — extension points are picked up at
   plugin load.

**No diagnostics/completion (the LSP half).**

- The server is spawned as `bundle exec her lsp ...` (or `her lsp ...`
  without a Gemfile) *in the project root*. Run the same command in a
  terminal at that directory — if it fails there, fix that first (most
  often: `bundle install` not run, or the boot file path is wrong).
- IDEs launched from the dock/Finder don't always see version-manager
  PATHs (mise, rbenv, asdf shims). If the command works in your terminal
  but the IDE shows nothing, check `Help → Show Log in …` (idea.log) for
  a spawn failure mentioning `bundle` or `her`, and launch the IDE from
  a terminal once to compare.
- In *this* repo, the boot file is the example app's:
  the checked-in `.her-lsp` already points at
  `examples/sinatra_app/config/boot.rb`, so opening the repo root works.
  The equivalent CLI check is
  `bundle exec her check -r examples/sinatra_app/config/boot.rb`.
- Without any boot file you still get parse/compile diagnostics — but
  only after the server starts, which happens when the first `.her` file
  is opened.

## Limits of "thin"

The plugin contains no language model — everything flows through LSP, so
IDE-native features that need a PSI tree (rename refactoring across
templates, structure view, cross-language find-usages) are out of scope
by design.
